/*
 * 1단계 예제 11: 주석 (Comments)
 * 
 * 코드에 설명을 추가하는 주석의 사용법을 학습합니다.
 * 주석은 컴파일되지 않으며 코드 이해를 돕습니다.
 */

#include <iostream>
using namespace std;

// 한 줄 주석: // 기호를 사용합니다
// 이 줄 전체가 주석입니다

int main() {
    // 변수 선언 주석
    int age = 25;        // 나이를 저장하는 변수
    
    /*
     * 여러 줄 주석: /* */ 형식을 사용합니다
     * 여러 줄에 걸쳐 설명을 작성할 수 있습니다
     * 주석은 컴파일 시 무시됩니다
     */
    
    cout << "나이: " << age << endl;
    
    // 주석의 용도:
    // 1. 코드 설명
    int result = age * 2;  // 나이에 2를 곱한 값
    
    // 2. TODO 표시
    // TODO: 나중에 검증 로직 추가 필요
    
    // 3. 코드 비활성화 (디버깅용)
    // cout << "디버그 메시지" << endl;
    
    // 4. 복잡한 로직 설명
    // 피타고라스 정리: a^2 + b^2 = c^2
    int a = 3, b = 4, c = 5;
    bool isRightTriangle = (a*a + b*b == c*c);
    
    /*
     * 주석 작성 가이드:
     * - 명확하고 간결하게 작성
     * - 왜(Why)를 설명하는 것이 중요
     * - 무엇(What)은 코드로 표현
     * - 너무 많은 주석은 오히려 방해가 될 수 있음
     */
    
    return 0;
}

// 함수 설명 주석 예제
/**
 * 두 수를 더하는 함수
 * @param a 첫 번째 정수
 * @param b 두 번째 정수
 * @return 두 수의 합
 */
int add(int a, int b) {
    return a + b;
}
